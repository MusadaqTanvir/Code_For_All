#Defining the tuples
dimensions = (100, 200)
print(dimensions)
#we can get each element of tuples by indexing as same in the list..
print(dimensions[0])
print(dimensions[1])
# We can also use the for loop to traverse the tuples..
for value in dimensions:
    print(value)